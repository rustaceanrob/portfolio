import Main from './Main'
import Header from './Header'

type Props = {}

const Root = (props: Props) => {
    return (
        <div>
            <Header/>
            <Main/>
        </div>
    )
}

export default Root